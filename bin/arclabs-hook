#!/bin/bash

# Run a named hook, like post-update (available in ~/.config/arclabs-os/hooks/post-update).

set -e

if [[ $# -lt 1 ]]; then
  echo "Usage: arclabs-hook [name] [args...]"
  exit 1
fi

HOOK=$1
HOOK_PATH="$HOME/.config/arclabs-os/hooks/$1"
shift

if [[ -f $HOOK_PATH ]]; then
  bash "$HOOK_PATH" "$@"
fi
